export const COLORS = {
  background: "#FFFFFFFF",      // Main app background
  primaryText: "#1A1A1A",      // Main text color (headings, primary content)
  secondaryText: "#6B7280",    // Secondary text (subtitles, muted info)
  icon: "#000000",             // Default icon color
  active: "#1A1A1A",           // Active tab/item color
  accent: "#3B82F6",           // Primary accent (buttons, highlights, links)
  accentLight: "#E0F2FE",      // Light accent backgrounds (chips, tags, subtle backgrounds)
  accentDark: "#1E40AF",       // Dark accent (active states, accent borders)
  success: "#22C55E",          // Success state (toasts, icons, buttons)
  successLight: "#D1FAE5",     // Success background (alerts, badges)
  warning: "#F59E42",          // Warning state (toasts, icons, buttons)
  warningLight: "#FEF3C7",     // Warning background (alerts, badges)
  error: "#EF4444",            // Error state (toasts, icons, buttons)
  errorLight: "#FEE2E2",       // Error background (alerts, badges)
  info: "#0EA5E9",             // Info state (toasts, icons, buttons)
  infoLight: "#E0F2FE",        // Info background (alerts, badges)
  card: "#F8F9FA",             // Card and surface backgrounds
  border: "#E5E7EB",           // Border color for cards, inputs, dividers
  shadow: "#000000",           // Shadow color for elevation
  white: "#FFFFFF",            // Pure white (overlays, text on dark backgrounds)
  black: "#000000",            // Pure black (text on light backgrounds)
  inactive: "#9CA3AF",         // Inactive tab/item color
  inactiveLight: "#E5E5EA",    // Inactive background (disabled buttons, muted chips)
  overlay: "rgba(0,0,0,0.2)",  // Overlay for modals, loading, etc.
  transparent: "transparent",   // For hiding or overlaying elements
};